<!doctype html>
<html class="no-js" lang="{{ request.locale.iso_code }}">
  <head>
    <title>{{page_title}}
    </title>
    {{content_for_header}}
  </head>
  <body>

    <main role="main">
      {{ content_for_layout }}
    </main>

  </body>
</html>
